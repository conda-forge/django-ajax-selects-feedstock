{% set version = "1.9.0" %}

package:
  name: django-ajax-selects
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/d/django-ajax-selects/django-ajax-selects-{{ version }}.tar.gz
  sha256: cc2e9b4936601dcdb9c578634f0b70a1b0da50c3e7e13868397d31545033dee5

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  build:
    - python
    - pip
  run:
    - python
    - django

test:
  imports:
    - ajax_select

about:
  home: https://github.com/crucialfelix/django-ajax-selects/
  license: MIT
  license_file: '{{ environ["RECIPE_DIR"] }}/LICENSE'
  summary: 'Edit ForeignKey, ManyToManyField and CharField in Django Admin using jQuery UI AutoComplete.'

extra:
  recipe-maintainers:
    - ocefpaf
